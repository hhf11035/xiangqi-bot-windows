# Windows 使用说明

1. 桌面交付版已经包含可用的 `.venv` Python 环境，通常无需安装任何依赖。
2. 如果 `.venv` 被删除或换到另一台电脑，请安装 64 位 Python 3.11 或更高版本，再右键 `setup_windows.ps1` 并选择“使用 PowerShell 运行”。
3. 打开电脑版微信，在独立窗口中打开“天天象棋”，保持窗口可见且不要最小化。
   运行期间不要让其他窗口遮挡棋盘；截图来自棋盘当前占用的屏幕区域。
4. 双击 `诊断.bat`。它只检查并截图，不会点击棋盘或自动落子。
5. 诊断通过后，先进入一盘棋，再双击 `启动单局.bat`。

如果落子位置有固定偏移，请关闭程序并双击 `重新校准并启动.bat`。按屏幕提示，分别把鼠标放在棋盘左上角棋子中心和右下角棋子中心；程序会保存这台电脑当前窗口的专用校准值。系统缩放为 125% 或 150% 也已支持。

停止程序：按 `Ctrl+C`，或把鼠标快速移到屏幕左上角。

若提示找不到窗口，请在 PowerShell 中先运行：

```powershell
$env:XIANGQI_WINDOW_TITLE = '窗口标题中的独特文字'
.\启动单局.bat
```

默认只接受 `Weixin.exe`、`WeChat.exe`、`WeixinAppEx.exe` 或 `WeChatAppEx.exe` 创建的窗口，避免误选标题中碰巧出现“天天象棋”的浏览器。如果你的微信进程名不同，可另设：`$env:XIANGQI_WINDOW_PROCESS = '实际进程名.exe'`。

`启动连续对局.bat` 会尝试自动处理结算弹窗并继续下一局，属于实验功能，首次使用建议先运行单局模式。
