# 天天象棋机器人——安全增强版使用说明与注意事项

## 一、版本说明

本程序通过截取天天象棋窗口、识别棋盘、调用本地 Pikafish 引擎计算着法，再模拟鼠标完成落子。

当前版本增加了以下安全保护：

- 窗口移动或缩放后自动同步棋盘坐标；
- 红黑方需要由将帅位置和全盘棋子分布共同确认；
- 对手走子必须唯一对应一步合法着法；
- 只有识别结果与预期棋盘一致，并且回合已经交给对手，才认为落子成功；
- 点击失败只重试原着一次，不会擅自改走其他着法；
- 识别、方向或落子结果不确定时会停止，不会继续碰运气；
- 提供只计算、不点击的观察模式。

这些保护可以显著减少错误，但视觉识别程序仍不能保证绝对零错误。

## 二、使用前准备

1. 打开电脑版微信和“天天象棋”小程序。
2. 将天天象棋窗口完整放在屏幕内，不要让棋盘、双方头像或计时器被遮挡。
3. 不要最小化窗口。
4. 进入一盘已经正式开始的棋局；棋盘中央不能仍显示“开始”按钮。
5. 运行程序后，不要再移动或缩放天天象棋窗口。虽然程序支持自动同步坐标，但固定窗口最稳定。

## 三、首次使用步骤

### 1. 运行诊断

双击 `诊断.bat`。

确认窗口检测、截图、CNN 模型和 Pikafish 引擎全部通过。诊断只检查环境，不会自动落子。

### 2. 优先使用观察模式

双击 `启动观察模式.bat`。

观察模式会识别棋盘并输出 Pikafish 建议着法，但不会点击棋盘。第一次使用、窗口尺寸改变、重新校准或程序更新后，都应先使用观察模式。

请人工确认：

- 程序显示的执红或执黑与实际一致；
- 控制台打印的棋盘与屏幕棋盘一致；
- 建议着法的起点确实存在己方棋子；
- 建议着法符合当前局面。

以上项目全部正确后，才能考虑使用自动落子模式。

### 3. 启动单局自动模式

双击 `启动单局.bat`。

程序会自动识别棋盘、判断红黑方、等待己方回合、计算并执行着法。一局结束后，应关闭当前命令窗口；开始下一局后重新启动程序。

## 四、重新校准

出现以下情况时应重新校准：

- 点击位置存在固定偏移；
- 更改了天天象棋窗口尺寸；
- 更改了系统显示缩放；
- 程序提示校准失效或棋盘识别无效。

双击 `重新校准并启动.bat`。程序会优先自动识别棋盘。

如果自动校准失败，程序才会进入手动校准：

1. 只移动鼠标，不要点击棋盘；
2. 将鼠标移到棋盘左上角网格交点，按 Enter；
3. 将鼠标移到棋盘右下角网格交点，按 Enter；
4. 左上角坐标必须同时位于右下角坐标的左侧和上方。

无效、反向、越界或尺寸异常的校准不会被保存。

## 五、运行中的控制方式

- `Ctrl+C`：正常停止程序；
- 将鼠标快速移到整个屏幕左上角：触发紧急停止；
- `F8`：开启或关闭额外随机 2～10 秒搜索时间；当前默认关闭。开启后这段时间会用于 Pikafish 计算，不再空等。

需要停止时，应优先按 `Ctrl+C`。不要直接强制关闭微信进程。

## 六、出现黄色警告或以下提示时应立即停止测试

- `cannot safely determine our side`：无法可靠确定红黑方；
- `orientation signals conflict`：方向判断信号冲突；
- `board recognition is invalid`：棋盘识别结果不合法；
- `opponent move was not one unique legal transition`：无法唯一确认对手着法；
- `move ... was not confirmed`：程序未能确认己方落子成功；
- `No legal engine move`：当前识别局面没有合法引擎着法；
- 大量出现 `FEN fix`、`Double-shot` 或低置信度修正；
- 程序显示的红黑方、棋盘或棋子位置与实际不一致。

出现这些提示后，不要在同一局中反复重新启动程序。应退出当前测试，检查窗口、校准和棋盘识别。

## 七、重要注意事项

1. 不要在铜钱场、排位赛或任何有损失的对局中进行程序测试。
2. 先使用观察模式，再使用无铜钱的人机、练习或双方同意的测试对局。
3. 不要遮挡棋盘、双方头像和绿色回合框。
4. 不要在运行时切换棋子皮肤、棋盘主题、窗口布局或系统缩放。
5. 不要在棋盘动画、弹窗、结算界面或“开始”按钮仍显示时启动自动模式。
6. 程序停止是安全保护，不代表应该立即绕过提示重新运行。
7. `ONNX not available` 不是故障；程序会自动使用 PyTorch CNN。
8. 当前 `启动连续对局.bat` 属于实验功能，不建议用于需要稳定性的测试。
9. Pikafish 在本机离线计算，但天天象棋对局本身可能联网；请遵守平台规则。
10. 任何自动化视觉程序都可能受到网络延迟、界面更新、遮挡、动画和新棋子皮肤影响。

## 八、推荐的安全测试顺序

1. `诊断.bat`；
2. `启动观察模式.bat`，核对红黑方、棋盘和建议着法；
3. 无铜钱对局中运行 `启动单局.bat`；
4. 连续完成多局且没有方向错误、识别错误或落子确认失败后，再评估是否继续使用；
5. 即使测试通过，也不建议在存在经济损失或账号处罚风险的场景中使用。

## 九、文件说明

- `启动观察模式.bat`：只识别和计算，不自动点击；
- `启动单局.bat`：单局自动落子；
- `启动连续对局.bat`：实验性连续对局；
- `重新校准并启动.bat`：强制重新校准后启动；
- `诊断.bat`：检查窗口、截图、CNN 和 Pikafish；
- `calib.json`：当前窗口的棋盘校准数据；
- `xiangqi_bot.py`：单局机器人主程序。
